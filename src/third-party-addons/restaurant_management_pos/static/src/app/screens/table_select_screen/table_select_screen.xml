<?xml version="1.0" encoding="UTF-8"?>
<templates id="template" xml:space="preserve">
    <t t-name="point_of_sale.TableSelectScreen">
        <div class="table-select-screen">
            <div class="pos-content-header">
                <div class="button-back" t-on-click="() => this.pos.showScreen('ProductScreen')">
                    <i class="fa fa-angle-left" role="img" aria-label="Back"/>
                </div>
                <h1 class="title">Seleccionar Mesa</h1>
            </div>
<!-- 
            <div class="floor-selector">
                <ul class="nav nav-pills" role="tablist">
                    <li class="nav-item" role="presentation">
                        <a class="nav-link active" data-bs-toggle="tab" href="#" role="tab">Salón Principal</a>
                    </li>
                    <li class="nav-item" role="presentation">
                        <a class="nav-link" data-bs-toggle="tab" href="#" role="tab">Terraza</a>
                    </li>
                    <li class="nav-item" role="presentation">
                        <a class="nav-link" data-bs-toggle="tab" href="#" role="tab">Barra</a>
                    </li>
                </ul>
            </div> -->
            
            <div class="tables-grid-container">
                <div class="tables-grid">
                    <t t-foreach="tables" t-as="table" t-key="table.id">
                        <div class="table-card" t-on-click="() => this.selectTable(table.id)">
                            <div class="table-name" t-esc="table.name"/>
                            <div class="table-info">
                                <span class="table-capacity" t-esc="'Capacidad: ' + str(table.capacity) + ' personas'" t-raw="true"/>
                                <span class="table-status" t-esc="table.status"/>
                            </div>
                        </div>
                    </t>
                </div>
            </div>
        </div>
    </t>
</templates>